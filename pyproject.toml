[project]
name = "fastapi-task"
version = "0.1.0"
description = "FastAPI task execution framework"
readme = "README.md"
requires-python = ">=3.10"
dependencies = [
    "fastapi>=0.138.1",
    "uvicorn>=0.20.0",
    "pydantic-settings>=2.0.0",
    "requests",
    "pandas",
    "openpyxl",
    "xlrd",
    "pypdf",
]
