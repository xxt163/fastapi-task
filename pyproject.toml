[project]
name = "fastapi-task"
version = "0.1.0"
description = "FastAPI task execution framework"
readme = "README.md"
requires-python = ">=3.10"
dependencies = [
    "fastapi[standard]>=0.138.1",
]
